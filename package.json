{
  "name": "mongo-cluster-monitor",
  "version": "1.1.0",
  "description": "Real-time MongoDB sharded cluster monitor — config RS, shard RS, router",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file=.env server.js",
    "dev": "node --watch  --env-file=.env server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongodb": "^6.3.0",
    "nodemailer": "^8.0.2",
    "express-rate-limit": "^8.3.1"
  }
}
